import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Compare & Contrast — StudyBoost', description: 'AI generates detailed comparisons between any two topics.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
