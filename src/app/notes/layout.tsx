import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Notes — StudyBoost', description: 'Create, organize, and search notes with tags and export.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
