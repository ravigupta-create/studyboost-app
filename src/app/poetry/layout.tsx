import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Poetry Analyzer — StudyBoost', description: 'Analyze poems for rhyme scheme, meter, literary devices, themes, and more.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
