import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Color Notes — StudyBoost', description: 'Note-taking with color-coded highlights for definitions, key terms, examples, and questions.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
