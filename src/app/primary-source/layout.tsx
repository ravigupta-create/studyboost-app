import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Primary Source Analyzer — StudyBoost', description: 'AI analyzes primary source documents for historical context, bias, and significance.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
