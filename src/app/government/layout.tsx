import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'US Government Reference — StudyBoost', description: 'Reference guide for US Government: branches, Bill of Rights, and all 27 amendments.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
