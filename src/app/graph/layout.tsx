import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Graphing Calculator — StudyBoost', description: 'Plot mathematical functions on an interactive canvas with zoom controls.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
