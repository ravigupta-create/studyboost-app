import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Lecture Notes — StudyBoost', description: 'Take timestamped notes during lectures with timer and session management.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
