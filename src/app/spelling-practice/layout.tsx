import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Spelling Practice — StudyBoost', description: 'Practice spelling with word banks by difficulty and audio pronunciation.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
