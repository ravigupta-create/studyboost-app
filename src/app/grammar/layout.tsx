import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Grammar Checker — StudyBoost', description: 'Identify grammar, punctuation, and style errors with detailed explanations.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
