import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Stoichiometry Solver — StudyBoost', description: 'Solve stoichiometry problems step-by-step with mole conversions and unit cancellation.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
