import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Focus Log — StudyBoost', description: 'Track distractions during study sessions and identify patterns.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
