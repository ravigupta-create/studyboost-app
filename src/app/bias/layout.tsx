import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Bias Detector — StudyBoost', description: 'AI analyzes text for biases and suggests more balanced perspectives.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
