import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Trivia Challenge — StudyBoost', description: 'AI-generated trivia questions across multiple categories with scoring.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
