import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Statistics Calculator — StudyBoost', description: 'Calculate mean, median, mode, standard deviation, quartiles and visualize with box plots.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
