import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Session Log — StudyBoost', description: 'Log study sessions and track time, techniques, and effectiveness.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
