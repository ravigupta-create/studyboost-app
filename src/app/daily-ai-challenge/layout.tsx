import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Daily AI Challenge — StudyBoost', description: 'Daily AI-generated quiz with questions across multiple subjects. One attempt per day!' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
