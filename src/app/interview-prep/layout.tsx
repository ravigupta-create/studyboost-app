import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Interview Prep — StudyBoost', description: 'AI generates interview questions with STAR method answer frameworks.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
