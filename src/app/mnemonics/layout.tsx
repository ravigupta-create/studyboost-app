import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Mnemonic Creator — StudyBoost', description: 'Create memorable mnemonics using acronyms, stories, and visual associations.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
