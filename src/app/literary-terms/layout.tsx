import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Literary Terms — StudyBoost', description: 'Searchable glossary of 100+ literary terms with definitions and examples.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
