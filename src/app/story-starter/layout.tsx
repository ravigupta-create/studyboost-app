import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Story Starter — StudyBoost', description: 'Generate creative story starters with character sketches and settings by genre and mood.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
