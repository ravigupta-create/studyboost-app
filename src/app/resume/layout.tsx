import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Resume Builder — StudyBoost', description: 'Build a student resume with education, experience, activities, and skills.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
