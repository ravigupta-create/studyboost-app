import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Retrieval Quiz — StudyBoost', description: 'Generate open-ended recall questions from your study material.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
