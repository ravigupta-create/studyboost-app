import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Active Recall Generator — StudyBoost', description: 'Generate fill-in-the-blank and short-answer questions from your notes to boost retention.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
