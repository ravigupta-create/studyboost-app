import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Data Interpreter — StudyBoost', description: 'AI analyzes datasets to identify trends, outliers, and draw conclusions.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
