import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Periodic Table — StudyBoost', description: 'Interactive periodic table with all 118 elements, search, and detail views.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
