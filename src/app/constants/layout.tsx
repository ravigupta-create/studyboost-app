import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Science Constants — StudyBoost', description: 'Reference for physics, chemistry, and math constants with formulas.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
