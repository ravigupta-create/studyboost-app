import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Lab Report Generator — StudyBoost', description: 'Generate a structured scientific lab report from your experiment topic and results.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
