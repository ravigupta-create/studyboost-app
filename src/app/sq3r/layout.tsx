import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SQ3R Reading Method — StudyBoost', description: 'Guided 5-step reading comprehension: Survey, Question, Read, Recite, Review.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
