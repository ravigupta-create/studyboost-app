import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Idiom Explorer — StudyBoost', description: 'AI explains idioms with origins, usage examples, and cross-language equivalents.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
