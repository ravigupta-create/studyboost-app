import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Transition Words — StudyBoost', description: 'Searchable categorized list of 200+ transition words for better writing.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
