import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'College Essay Helper — StudyBoost', description: 'AI brainstorming, angle suggestions, and outlines for Common App essay prompts.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
