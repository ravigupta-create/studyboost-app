import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Class Schedule — StudyBoost', description: 'Visual weekly class schedule with color-coded time blocks.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
