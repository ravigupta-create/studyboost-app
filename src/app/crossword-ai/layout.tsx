import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'AI Crossword — StudyBoost', description: 'AI generates crossword puzzles on any topic for interactive learning.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
