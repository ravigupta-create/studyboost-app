import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Geometry Explainer — StudyBoost', description: 'Get geometry theorems explained with proofs, worked examples, and practice problems.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
