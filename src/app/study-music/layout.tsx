import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Study Music — StudyBoost', description: 'Ambient sound generator with white noise, brown noise, rain, and binaural beats.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
