import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Question Predictor — StudyBoost', description: 'AI predicts likely exam questions based on your course topics.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
