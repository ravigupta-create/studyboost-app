import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Knowledge Graph — StudyBoost', description: 'Extract key concepts and relationships from your notes as a structured knowledge graph.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
