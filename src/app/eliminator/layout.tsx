import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Answer Eliminator — StudyBoost', description: 'Strategically eliminate wrong MC answers with AI reasoning.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
