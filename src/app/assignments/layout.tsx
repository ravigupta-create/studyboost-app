import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Assignment Tracker — StudyBoost', description: 'Track assignments with priorities, due dates, and completion status.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
