import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Spaced Repetition — StudyBoost', description: 'SM-2 spaced repetition flashcard reviewer with intelligent scheduling.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
