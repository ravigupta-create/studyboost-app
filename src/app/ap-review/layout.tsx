import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'AP Exam Review — StudyBoost', description: 'Generate AP exam-style practice questions for any AP subject.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
