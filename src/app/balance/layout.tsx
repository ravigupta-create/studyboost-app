import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Equation Balancer — StudyBoost', description: 'Balance chemical equations step-by-step with atom counts and reaction type identification.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
