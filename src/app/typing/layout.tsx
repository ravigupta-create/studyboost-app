import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Typing Test — StudyBoost', description: 'Test your typing speed with academic passages and real-time feedback.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
