import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Verb Conjugator — StudyBoost', description: 'AI generates full conjugation tables for verbs in Spanish, French, German, and Italian.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
