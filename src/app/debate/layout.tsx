import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Debate Prep — StudyBoost', description: 'Generate arguments for and against any debate topic with evidence and rebuttals.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
