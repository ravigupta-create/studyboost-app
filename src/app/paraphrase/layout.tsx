import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Smart Paraphraser — StudyBoost', description: 'Rewrite text in different styles: simpler, formal, concise, or academic.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
