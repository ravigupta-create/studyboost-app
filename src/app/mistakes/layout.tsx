import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Mistake Logger — StudyBoost', description: 'Track and analyze your mistakes to identify patterns and improve.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
