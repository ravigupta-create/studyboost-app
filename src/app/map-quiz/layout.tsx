import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Map Quiz — StudyBoost', description: 'Geography quiz covering 50 countries and their capitals.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
