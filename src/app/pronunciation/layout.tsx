import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Pronunciation Guide — StudyBoost', description: 'AI provides IPA transcription, syllable breakdowns, and pronunciation tips.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
