import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Weak Topic Analyzer — StudyBoost', description: 'AI analyzes your performance to identify weak areas and suggest study priorities.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
