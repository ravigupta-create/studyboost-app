import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Source Evaluator — StudyBoost', description: 'Evaluate source reliability using the CRAAP test with AI analysis.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
