import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Text-to-Speech Reader — StudyBoost', description: 'Listen to text read aloud with customizable voice, speed, and pitch.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
