import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Thesis Generator — StudyBoost', description: 'Generate strong thesis statements with analysis of strengths and weaknesses.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
