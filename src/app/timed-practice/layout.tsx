import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Timed Practice — StudyBoost', description: 'Take timed multiple choice quizzes on any subject with AI-generated questions.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
