import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Argument Builder — StudyBoost', description: 'Build strong arguments using the Toulmin model with AI assistance.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
