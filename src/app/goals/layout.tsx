import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SMART Goals — StudyBoost', description: 'Create SMART goals with milestones, progress tracking, and streaks.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
