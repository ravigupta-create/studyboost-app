import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Score Predictor — StudyBoost', description: 'Track practice test scores and predict future performance with linear regression.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
