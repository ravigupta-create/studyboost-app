import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Study Journal — StudyBoost', description: 'Daily reflection journal with guided prompts and mood tracking.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
