import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Habit Tracker — StudyBoost', description: 'Track daily and weekly habits with streaks and heatmap visualization.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
