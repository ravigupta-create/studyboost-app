import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Significant Figures — StudyBoost', description: 'Count significant figures and perform operations with correct sig fig rules.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
