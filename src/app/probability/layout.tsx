import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Probability Calculator — StudyBoost', description: 'Calculate permutations, combinations, binomial probability, and factorials.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
