import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Element Quiz — StudyBoost', description: 'Test your knowledge of the periodic table with multiple choice questions.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
