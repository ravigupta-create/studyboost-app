import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Dyslexia Reader — StudyBoost', description: 'Dyslexia-friendly text reader with adjustable formatting and reading ruler.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
