import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Study Streaks — StudyBoost', description: 'Track your study streaks, XP, and achievement badges.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
