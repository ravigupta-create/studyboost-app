import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Conjugation Tables — StudyBoost', description: 'Verb conjugation tables and practice for Spanish, French, and German.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
