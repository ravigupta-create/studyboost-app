import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Logical Fallacy Finder — StudyBoost', description: 'AI identifies logical fallacies in arguments and explains how to fix them.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
