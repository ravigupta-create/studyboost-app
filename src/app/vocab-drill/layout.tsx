import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Vocab Drill — StudyBoost', description: 'Speed vocabulary drill to test word recall from definitions.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
