import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Base Converter — StudyBoost', description: 'Convert numbers between binary, octal, decimal, and hexadecimal with step-by-step explanations.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
