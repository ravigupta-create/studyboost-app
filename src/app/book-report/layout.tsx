import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Book Report Helper — StudyBoost', description: 'Generate a structured book report template with summary, themes, and character analysis.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
