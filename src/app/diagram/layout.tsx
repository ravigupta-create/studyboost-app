import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Sentence Diagrammer — StudyBoost', description: 'AI breaks down sentences into grammatical components with explanations.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
