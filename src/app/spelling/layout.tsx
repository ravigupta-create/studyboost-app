import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Spelling Bee — StudyBoost', description: 'AI-generated spelling practice with definitions and graded difficulty.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
