import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Calculus Solver — StudyBoost', description: 'Solve derivatives, integrals, and limits step-by-step with proper math notation.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
