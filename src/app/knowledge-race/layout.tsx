import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Knowledge Race — StudyBoost', description: 'Rapid-fire AI quiz with streak multipliers. 3 wrong answers and you are out!' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
