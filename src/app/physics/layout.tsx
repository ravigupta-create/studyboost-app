import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Physics Solver — StudyBoost', description: 'Solve physics problems step-by-step with formulas, units, and clear explanations.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
