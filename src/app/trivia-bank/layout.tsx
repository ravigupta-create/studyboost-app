import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Trivia Bank — StudyBoost', description: '100 trivia questions across 5 categories with score tracking.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
