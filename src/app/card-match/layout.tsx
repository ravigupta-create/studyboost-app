import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Card Match Game — StudyBoost', description: 'Memory matching game with custom word-definition pairs.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
