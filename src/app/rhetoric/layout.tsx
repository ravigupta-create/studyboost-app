import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Rhetorical Analysis — StudyBoost', description: 'Analyze rhetorical devices, tone, audience, and persuasive techniques in any text.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
