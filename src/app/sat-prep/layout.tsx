import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SAT Prep — StudyBoost', description: 'Generate SAT-style practice questions by section and difficulty level.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
