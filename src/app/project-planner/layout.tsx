import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Project Planner — StudyBoost', description: 'Create and manage projects with tasks, progress tracking, and time estimates.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
