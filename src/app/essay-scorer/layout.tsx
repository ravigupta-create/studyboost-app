import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Essay Scorer — StudyBoost', description: 'Score your essay on 6 writing traits with detailed feedback and improvement tips.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
