import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Text Annotator — StudyBoost', description: 'Generate detailed annotations for any passage with vocabulary, context, and literary devices.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
