import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Crossword Generator — StudyBoost', description: 'Generate crossword puzzles from your own words and clues.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
