import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Cause & Effect Mapper — StudyBoost', description: 'AI maps cause and effect chains for events, processes, and phenomena.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
