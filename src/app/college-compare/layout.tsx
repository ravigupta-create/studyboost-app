import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'College Compare — StudyBoost', description: 'Compare colleges side-by-side with tuition, acceptance rates, and personal rankings.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
