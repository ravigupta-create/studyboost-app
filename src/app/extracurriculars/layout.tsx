import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Extracurriculars Tracker — StudyBoost', description: 'Track activities, roles, hours, and achievements for college applications.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
