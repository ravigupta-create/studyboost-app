import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Unit Converter — StudyBoost', description: 'Convert between units of length, mass, temperature, volume, speed, and more.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
